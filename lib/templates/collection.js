
var app = app || {};

(function () {
	'use strict';

	// <%= Name %> Collection
	// ---------------

	app.<%= Name %>Collection = Backbone.Collection.extend({
		// Reference to this collection's model.
		model: app.<%= Name %>Model,

	});

})();
